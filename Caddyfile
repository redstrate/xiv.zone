{
    admin off # we dont use this so lol
}

:9455 {
    header {
        -Server

        Cache-Control "max-age=604800, immutable"
    }

    root /astra-distrib/* /var/astra-distrib

    encode gzip

    file_server
}

